name: 'Setup streamcmd'
description: A GitHub Action to setup the steamcmd command alias.
branding:
  color: 'blue'
  icon: 'terminal'
runs:
  using: 'node20'
  main: 'dist/index.js'
  post: 'dist/index.js'
